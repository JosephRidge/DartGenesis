void main() {
  // LISTS( ordered,repeated)
  var numbers = [0, 1, 2, 3, 4, 5, 4, 5];
  List<String> fruits = ["mango", "apple", "pear"];
 

  // sets (unordered, unique)
  var names = {"John", "Doe"};
  Set<String> firstNames = {"John", "Doe"};

  // MAP(key:value pairs) ->objects/ models
  var person = {"name": "John", "age": 100, "caree": "Engineer"};
  Map<String, String> child = {
    "name": "John",
    "age": "one year",
    "career": "child",
  };
 
  Map car = { // Map<dynamic, dynamic> car = 
    "model":"Mazda",
    "yom":2000,
    "color":"blue"
  };
}
